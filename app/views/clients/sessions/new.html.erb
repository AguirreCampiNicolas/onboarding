<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
</head>
<body class="bg-gray-100">
  <div class="flex h-screen">
    <!-- Left side -->
    <%= render 'clients/shared/left_panel_new_session_and_registration' %>
    <!-- Right side -->
    <div class="w-1/2 bg-white flex flex-col justify-center items-center p-10">
      <h4 class="text-h4-lg lg:text-h4-lg md:text-h4-sm sm:text-h4-sm">¡Hola, otra vez!</h4>
      <p class="text-lg lg:text-text-lg md:text-sm sm:text-sm">Por favor, confirma tu información para ingresar a Plan IT.</p>
      <%= form_with(resource: resource, as: resource_name, url: session_path(resource_name), local: true) do |f| %>
        <div class="mb-4 w-2/3">
          <%= f.label :email, class: "block text-gray-700 mb-2" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full p-2 border rounded" %>
        </div>
        <div class="mb-4 w-2/3">
          <%= f.label :password, class: "block text-gray-700 mb-2" %>
          <%= f.password_field :password, autocomplete: "current-password", class: "w-full p-2 border rounded" %>
        </div>
        <div class="mb-4 w-2/3">
          <%= f.submit "Iniciar sesión", class: "w-full bg-blue-500 text-white p-2 rounded cursor-pointer hover:bg-blue-700" %>
        </div>
      <% end %>
      <%= render "clients/shared/links" %>
    </div>
  </div>
</body>
</html>
