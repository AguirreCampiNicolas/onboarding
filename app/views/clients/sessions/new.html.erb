<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
</head>
<body>
    <%= render 'shared/mobile_navbar_login_signup' %>
  <div class="flex h-screen">
    <!-- Left side -->
    <%= render 'clients/shared/left_panel_new_session_and_registration' %>
    <!-- Right side -->
    <div class="w-full md:w-1/2 bg-white flex flex-col justify-center p-10">
      <%= form_with(model: resource, as: resource_name, url: session_path(resource_name), local: true, class: "flex flex-col justify-center w-full mt-20 2xl:px-60") do |f| %>
        <h4 class="text-h4-lg lg:text-h4-lg">¡Hola, otra vez!</h4>
        <p class="text-lg lg:text-text-lg mt-10">Por favor, confirma tu información para ingresar a Plan IT.</p>
        <div class="mb-4 w-full">
          <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "Email", class: "w-full bg-transparent border-0 border-b-2 focus:outline-none focus:ring-0 focus:border-black border-gray-200" %>
        </div>
        <div class="mb-4 w-full">
          <%= f.password_field :password, autocomplete: "current-password", placeholder: "Contraseña", class: "mt-5 w-full bg-transparent border-0 border-b-2 focus:outline-none focus:ring-0 focus:border-black border-gray-200" %>
        </div>
        <div class="mb-4 w-full">
          <%= f.check_box :remember_me, class: "mr-2" %>
          <%= f.label :remember_me, class: "text-gray-700" %>
        </div>
        <div class="mb-4 w-full mt-20 mx-auto">
          <%= f.submit "INICIAR SESIÓN", class: "w-full bg-planit-blue-gradient text-white p-2 rounded cursor-pointer hover:bg-blue-700 shadow-planit" %>
        </div>
      <% end %>
      <p class="text-planit-grey-2 text-small-text-lg text-center">¿No tienes una cuenta aún?</p>
      <%= link_to "¡Regístrate aquí!", new_registration_path(resource_name), class: "text-planit-dark-blue text-center" %><br />
    </div>
  </div>
</body>
</html>
